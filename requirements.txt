# requirements.txt
# 전체 기능 (표정 + 음성 + LSTM)
# Python 3.11 권장 (3.9~3.11 지원)
# ⚠️ Python 3.12/3.13은 tensorflow 호환 문제로 권장하지 않습니다.
#
# 설치: pip install -r requirements.txt
# 최소 설치: pip install -r requirements-minimal.txt
# STT 추가: pip install -r requirements-stt.txt
#
# 단계별 설치:
#   1. pip install -r requirements-minimal.txt  (UI + 카메라)
#   2. pip install -r requirements-face.txt     (표정 분석)
#   3. pip install -r requirements-voice.txt    (음성 분석)
#   4. pip install -r requirements-stt.txt      (한국어 STT)

# === 기본 (minimal과 동일) ===
streamlit==1.31.0
opencv-python==4.9.0.80
numpy==1.26.4
pandas==2.2.0
pyttsx3==2.98
python-dateutil==2.8.2

# === 표정 분석 ===
deepface==0.0.92

# === 음성 분석 ===
librosa==0.10.1
sounddevice==0.4.7
scikit-learn==1.4.0
joblib==1.3.2

# === 딥러닝 (LSTM) ===
# tensorflow는 deepface 설치 시 자동 포함됨

# === STT (선택적, 별도 설치 권장) ===
# pip install -r requirements-stt.txt
# openai-whisper==20231117
