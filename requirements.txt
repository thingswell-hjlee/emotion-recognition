# requirements.txt
# 전체 기능 (표정 분석 + 음성 분석 + LSTM 예측)
# Python 3.11 권장 (3.9~3.11 지원)
# ⚠️ Python 3.12/3.13은 tensorflow 호환 문제로 권장하지 않습니다.
#
# 설치: pip install -r requirements.txt
# 최소 설치: pip install -r requirements-minimal.txt

# === 기본 (minimal과 동일) ===
streamlit==1.31.0
opencv-python==4.9.0.80
numpy==1.26.4
pandas==2.2.0
pyttsx3==2.98
python-dateutil==2.8.2

# === 표정 분석 ===
deepface==0.0.92
# tensorflow는 deepface 설치 시 자동 포함됨
# 별도 지정 시: tensorflow==2.15.1

# === 음성 분석 ===
librosa==0.10.1
sounddevice==0.4.7

# === 머신러닝 (LSTM, 음성 분류) ===
scikit-learn==1.4.0
joblib==1.3.2

# === 선택적 (필요 시 수동 설치) ===
# mediapipe==0.10.9       # Python 3.11까지만 지원
# pyaudio==0.2.14         # Windows: pip install pipwin && pipwin install pyaudio
