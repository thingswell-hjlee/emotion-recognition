# Requirements - Multimodal Emotion State Monitor

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | multimodal-emotion-state-monitor |
| 목적 | 노트북 웹캠과 마이크를 통해 사용자의 얼굴 표정과 음성을 분석하여 감정 상태를 인식하고, LSTM 기반 예측 및 음성 안내를 제공하는 로컬 실행 프로그램 |
| 실행 환경 | 로컬 PC (오프라인 가능, AWS 미사용) |
| 개발 방식 | AWS AI-DLC 방식 기반 설계, 초기 MVP는 로컬 전용 |

## 2. 감정 분류 체계

| 감정 | 설명 |
|------|------|
| neutral | 무표정 / 중립 |
| happy | 행복 / 기쁨 |
| sad | 슬픔 |
| angry | 분노 |
| surprised | 놀람 |
| fearful | 공포 |
| disgusted | 혐오 |
| stressed | 스트레스 |
| calm | 평온 |

---

## 3. 기능 요구사항

---

### FR-01: 웹캠 기반 얼굴 감지

- **기능명**: 웹캠 기반 실시간 얼굴 감지
- **사용자 스토리**: 사용자로서, 프로그램이 시작되면 웹캠을 통해 내 얼굴을 자동으로 감지하여 표정 분석의 기반이 되길 원한다.
- **상세 설명**:
  - 노트북 내장 웹캠 또는 USB 웹캠에서 실시간으로 프레임을 캡처한다.
  - 각 프레임에서 얼굴 영역을 감지하고 바운딩 박스를 표시한다.
  - UI에서 시작 버튼을 누른 후에만 캡처를 시작한다.
  - 카메라 사용 중임을 UI에 명확히 표시한다.
- **입력값**: 웹캠 영상 스트림 (기본 장치 ID: 0)
- **출력값**: 감지된 얼굴 영역 좌표 (x, y, w, h)
- **승인 기준**:
  - 카메라가 정상적으로 열리고 프레임이 캡처된다.
  - 얼굴이 화면에 있을 때 1초 이내에 감지된다.
  - 여러 얼굴이 있을 경우 가장 큰(가까운) 얼굴을 우선 처리한다.
- **예외 상황**:
  - 카메라가 없거나 접근 불가 시 에러 메시지를 표시하고 표정 분석을 비활성화한다.
  - 다른 프로그램이 카메라를 점유 중일 경우 안내 메시지를 표시한다.
  - 얼굴이 감지되지 않을 경우 "얼굴 미감지" 상태를 UI에 표시한다.

---

### FR-02: 얼굴 표정 감정 분류

- **기능명**: 표정 기반 감정 상태 분류
- **사용자 스토리**: 사용자로서, 내 얼굴 표정을 분석하여 현재 감정 상태를 9가지 중 하나로 분류해 주길 원한다.
- **상세 설명**:
  - 감지된 얼굴 영역에 대해 딥러닝 모델을 사용하여 감정을 분류한다.
  - 분류 결과는 감정 레이블과 신뢰도 점수를 포함한다.
  - 분석 주기는 사용자가 설정한 간격(기본 10초)에 따라 수행한다.
  - 분석 결과를 UI의 현재 감정 상태 표시 영역에 반영한다.
- **입력값**: 감지된 얼굴 이미지 (OpenCV BGR numpy array)
- **출력값**: 감정 레이블 (string), 신뢰도 점수 (float, 0.0~1.0), 각 감정별 점수 딕셔너리
- **승인 기준**:
  - 9가지 감정 중 하나로 분류된다.
  - 신뢰도 점수가 함께 제공된다.
  - 분석 결과가 UI에 실시간으로 반영된다.
  - GPU 없이 CPU만으로도 분석이 수행된다.
- **예외 상황**:
  - 얼굴이 감지되지 않으면 분석을 건너뛴다.
  - 모델 로드 실패 시 에러 메시지를 표시하고 음성 분석만으로 전환을 안내한다.
  - 이미지 품질이 낮아 분석 불가 시 "분석 불가" 상태를 표시한다.

---

### FR-03: 마이크 기반 음성 데이터 수집

- **기능명**: 마이크 기반 실시간 음성 수집
- **사용자 스토리**: 사용자로서, 프로그램이 내 음성을 실시간으로 수집하여 음성 감정 분석의 기반이 되길 원한다.
- **상세 설명**:
  - 노트북 내장 마이크 또는 외부 마이크에서 오디오 스트림을 캡처한다.
  - 수집된 음성은 메모리에서만 처리하며 파일로 저장하지 않는다.
  - UI에서 시작 버튼을 누른 후에만 수집을 시작한다.
  - 마이크 사용 중임을 UI에 명확히 표시한다.
  - 분석 주기(기본 10초)에 맞춰 오디오 버퍼를 관리한다.
- **입력값**: 마이크 오디오 스트림 (기본 입력 장치)
- **출력값**: 오디오 데이터 버퍼 (numpy array, 샘플레이트 16kHz 또는 22kHz)
- **승인 기준**:
  - 마이크가 정상적으로 열리고 오디오가 캡처된다.
  - 설정된 주기(5~20초)에 맞춰 오디오 버퍼가 관리된다.
  - 원본 음성 파일은 저장되지 않는다.
- **예외 상황**:
  - 마이크가 없거나 접근 불가 시 에러 메시지를 표시하고 음성 분석을 비활성화한다.
  - 무음 상태가 지속되면 "음성 미감지" 상태를 UI에 표시한다.
  - 오디오 버퍼 오버플로우 시 가장 오래된 데이터를 폐기한다.

---

### FR-04: 음성 특징 기반 감정 분류

- **기능명**: 음성 특징 분석 기반 감정 상태 분류
- **사용자 스토리**: 사용자로서, 내 음성의 톤, 속도, 에너지 등을 분석하여 감정 상태를 분류해 주길 원한다.
- **상세 설명**:
  - 수집된 오디오 데이터에서 MFCC, 피치, 에너지 등 음성 특징을 추출한다.
  - 추출된 특징으로 감정을 9가지 중 하나로 분류한다.
  - librosa 또는 유사 라이브러리를 사용하여 특징을 추출한다.
  - 분석 결과를 UI의 현재 감정 상태 표시 영역에 반영한다.
- **입력값**: 오디오 데이터 버퍼 (numpy array)
- **출력값**: 감정 레이블 (string), 신뢰도 점수 (float, 0.0~1.0), 각 감정별 점수 딕셔너리
- **승인 기준**:
  - 9가지 감정 중 하나로 분류된다.
  - 신뢰도 점수가 함께 제공된다.
  - 무음이 아닌 유효한 음성에 대해서만 분석을 수행한다.
- **예외 상황**:
  - 무음 구간에서는 분석을 건너뛴다.
  - 음성 품질이 낮아(노이즈 과다) 분석 불가 시 "분석 불가" 상태를 표시한다.
  - 모델 로드 실패 시 에러 메시지를 표시한다.

---

### FR-05: 멀티모달 감정 통합 계산

- **기능명**: 표정 + 음성 감정 결과 통합
- **사용자 스토리**: 사용자로서, 표정과 음성 분석 결과를 종합하여 더 정확한 감정 상태를 확인하고 싶다.
- **상세 설명**:
  - 표정 감정 점수와 음성 감정 점수를 가중 평균으로 통합한다.
  - 기본 가중치: 표정 60%, 음성 40% (조정 가능).
  - 하나의 소스만 사용 가능한 경우 해당 소스의 결과만 사용한다.
  - UI에서 선택한 분석 모드(표정만/음성만/통합)에 따라 계산 방식이 달라진다.
- **입력값**: 표정 감정 점수 딕셔너리, 음성 감정 점수 딕셔너리, 분석 모드 설정
- **출력값**: 통합 감정 레이블 (string), 통합 신뢰도 (float), 통합 감정 점수 딕셔너리
- **승인 기준**:
  - 통합 모드에서 두 소스의 결과가 합리적으로 결합된다.
  - 단일 모드에서는 해당 소스의 결과만 반영된다.
  - 통합 결과가 UI에 표시된다.
- **예외 상황**:
  - 한쪽 소스가 "분석 불가" 상태이면 다른 소스의 결과만 사용한다.
  - 양쪽 모두 "분석 불가"이면 "분석 불가" 상태를 표시한다.

---

### FR-06: 주기적 감정 평균 계산

- **기능명**: 감정 인식 결과 주기별 평균 계산
- **사용자 스토리**: 사용자로서, 설정된 주기(기본 10초) 동안의 감정 결과 평균을 확인하여 일시적 변동이 아닌 전체적인 감정 추세를 파악하고 싶다.
- **상세 설명**:
  - 설정된 주기(5~20초, 기본 10초) 내 수집된 감정 분석 결과를 평균 계산한다.
  - 각 감정 카테고리별 점수를 시간 가중 평균으로 계산한다.
  - 평균 결과 중 가장 높은 점수의 감정을 대표 감정으로 선정한다.
  - 평균 결과를 UI의 "최근 평균 감정" 표시 영역에 반영한다.
- **입력값**: 주기 내 수집된 감정 결과 리스트 (List[EmotionResult]), 주기 설정값 (int, 초)
- **출력값**: 평균 감정 레이블 (string), 평균 신뢰도 (float), 감정별 평균 점수 딕셔너리
- **승인 기준**:
  - 설정된 주기마다 정확히 평균이 계산된다.
  - 주기 내 데이터가 없으면 이전 결과를 유지한다.
  - 평균 결과가 UI에 즉시 반영된다.
- **예외 상황**:
  - 주기 내 분석 결과가 하나도 없으면 "데이터 부족" 상태를 표시한다.
  - 주기 변경 시 현재 누적 데이터를 초기화하고 새 주기로 재시작한다.

---

### FR-07: 음성 안내 기능

- **기능명**: 감정 상태 음성 안내
- **사용자 스토리**: 사용자로서, 평균 감정 결과가 계산되면 짧은 음성 안내로 현재 상태를 알려주길 원한다.
- **상세 설명**:
  - 감정 평균이 계산된 후 상태에 맞는 짧은 문장을 음성으로 출력한다.
  - 안내 예시:
    - "현재 상태는 안정적으로 보입니다."
    - "약간 긴장된 상태로 보입니다."
    - "긍정적인 감정이 감지되었습니다."
    - "피로 또는 스트레스 신호가 있습니다."
    - "감정 변화가 커지고 있습니다."
  - 너무 자주 반복되지 않도록 최소 간격을 둔다 (연속 동일 메시지 억제).
  - 사용자가 음성 안내 사용 여부를 ON/OFF 할 수 있다.
  - 음성 출력 볼륨을 UI에서 0~100%로 조절 가능하다.
- **입력값**: 평균 감정 결과, 음성 안내 ON/OFF 설정, 볼륨 설정 (0~100)
- **출력값**: 음성 출력 (스피커), 안내 문장 텍스트 (로그)
- **승인 기준**:
  - 주기적으로 음성 안내가 출력된다.
  - 동일 메시지가 연속으로 3회 이상 반복되지 않는다.
  - 볼륨 조절이 즉시 반영된다.
  - ON/OFF 토글이 즉시 반영된다.
- **예외 상황**:
  - 스피커/오디오 출력 장치가 없으면 텍스트로만 안내하고 경고를 표시한다.
  - 음성 합성 엔진 초기화 실패 시 텍스트 안내로 대체한다.
  - 분석 중 음성 안내 출력 시 마이크 입력과 간섭하지 않도록 처리한다.

---

### FR-08: LSTM 기반 감정 예측

- **기능명**: LSTM 기반 다음 감정 상태 예측
- **사용자 스토리**: 사용자로서, 최근 감정 추이를 기반으로 다음에 올 감정 상태를 예측하여 감정 변화 방향을 미리 파악하고 싶다.
- **상세 설명**:
  - 최근 감정 시계열 데이터(표정 점수, 음성 점수, 통합 점수)를 입력으로 사용한다.
  - LSTM 모델이 다음 주기의 감정 상태를 예측한다.
  - 예측 주기는 기본 10초 단위이다.
  - 초기 MVP에서는 간단한 LSTM 구조와 더미/샘플 데이터 기반으로 구현한다.
  - 향후 실제 사용자 데이터 기반 학습 구조로 확장 가능하게 설계한다.
  - 예측 결과를 UI의 "LSTM 예측 감정" 표시 영역에 반영한다.
- **입력값**: 최근 N개 주기의 감정 시계열 데이터 (numpy array, shape: [sequence_length, features])
- **출력값**: 예측 감정 레이블 (string), 예측 신뢰도 (float), 예측 감정 점수 딕셔너리
- **승인 기준**:
  - 충분한 시계열 데이터(최소 5개 주기)가 쌓인 후 예측이 시작된다.
  - 예측 결과가 UI에 표시된다.
  - 모델이 CPU 환경에서도 10초 이내에 예측을 완료한다.
- **예외 상황**:
  - 시계열 데이터가 부족하면 "데이터 수집 중" 상태를 표시한다.
  - 모델 추론 실패 시 이전 예측 결과를 유지하고 경고를 표시한다.
  - 모델 파일이 없으면 예측 기능을 비활성화하고 안내 메시지를 표시한다.

---

### FR-09: 평균값과 예측값 비교 판단

- **기능명**: 감정 변화 방향 판단 (평균 vs 예측 비교)
- **사용자 스토리**: 사용자로서, 현재 평균 감정과 LSTM 예측 감정을 비교하여 감정이 어떤 방향으로 변하고 있는지 알고 싶다.
- **상세 설명**:
  - LSTM 예측값과 최근 평균값을 비교하여 감정 변화 방향을 판단한다.
  - 비교 결과 분류:
    - **안정적**: 평균과 예측이 유사
    - **긍정 방향 변화**: 예측이 평균보다 긍정적
    - **부정 방향 변화**: 예측이 평균보다 부정적
    - **스트레스 증가 가능성**: stressed/fearful 방향으로 예측
    - **감정 변동성 증가**: 변화 폭이 큰 경우
  - 비교 결과를 UI의 "비교 결과" 표시 영역에 반영한다.
- **입력값**: 평균 감정 점수 딕셔너리, 예측 감정 점수 딕셔너리
- **출력값**: 변화 방향 레이블 (string), 변화 크기 (float)
- **승인 기준**:
  - 5가지 변화 방향 중 하나로 분류된다.
  - 비교 결과가 UI에 즉시 표시된다.
  - 감정 변화가 클 때 음성 안내에도 반영된다.
- **예외 상황**:
  - 예측 데이터가 없으면 비교를 수행하지 않고 "대기 중" 상태를 표시한다.
  - 평균 데이터가 없으면 비교를 수행하지 않는다.

---

### FR-10: 기본 설정 UI

- **기능명**: 감정 인식 설정 및 모니터링 UI
- **사용자 스토리**: 사용자로서, 간단한 UI를 통해 분석 설정을 조정하고, 결과를 실시간으로 확인하며, 시작/정지를 제어하고 싶다.
- **상세 설명**:
  - UI 구성 요소:
    1. 감정 인식 주기 설정 (5~20초, 기본 10초)
    2. 음성 출력 볼륨 조정 (0~100%)
    3. 감정 분석 종류 선택 (표정만 / 음성만 / 표정+음성 통합)
    4. 시작 버튼
    5. 정지 버튼
    6. 현재 감정 상태 표시 영역
    7. 최근 평균 감정 표시 영역
    8. LSTM 예측 감정 표시 영역
    9. 평균값과 예측값 비교 결과 표시 영역
    10. 로그 표시 영역
  - 카메라/마이크 사용 중임을 명확히 표시한다.
  - "감정 분석 결과는 참고용이며 의학적·심리학적 진단이 아닙니다" 안내문을 포함한다.
- **입력값**: 사용자 설정 조작 (슬라이더, 버튼, 드롭다운)
- **출력값**: 실시간 감정 분석 결과 표시, 로그 메시지, 설정 상태 반영
- **승인 기준**:
  - 모든 10개 UI 구성 요소가 정상 동작한다.
  - 시작/정지 버튼이 즉시 반응한다.
  - 설정 변경이 즉시 반영된다.
  - 카메라/마이크 사용 상태가 명확히 표시된다.
  - 안내문이 항상 표시된다.
- **예외 상황**:
  - UI 렌더링 오류 시 기본값으로 복원한다.
  - 브라우저/윈도우 크기가 작을 경우 반응형으로 조정한다.
  - 분석 중 UI 조작이 분석을 방해하지 않도록 비동기 처리한다.

---

## 4. 비기능 요구사항

---

### NFR-01: 개인정보 및 보안

- 웹캠 영상은 로컬 메모리에서만 처리한다.
- 얼굴 이미지는 저장하지 않는다.
- 원본 음성 파일은 저장하지 않는다.
- 외부 서버나 클라우드로 영상/음성을 전송하지 않는다.
- 사용자가 시작 버튼을 누른 경우에만 분석을 시작한다.
- UI에 카메라와 마이크 사용 중임을 명확히 표시한다.
- 감정 분석 결과는 참고용이며 의학적·심리학적 진단이 아님을 안내한다.
- 감정 분석 결과와 통계 데이터만 로컬 로그로 저장할 수 있다.

### NFR-02: 성능

- 실시간에 가까운 영상 표시 (최소 10 FPS 이상)
- 감정 분석은 설정된 주기 내에 완료되어야 한다.
- LSTM 예측은 10초 이내에 완료되어야 한다.
- GPU 없이 CPU만으로 동작 가능해야 한다.
- 메모리 사용량 2GB 이하 유지 (모델 로드 포함)

### NFR-03: 호환성

- Python 3.9 이상에서 동작한다.
- Windows 10/11, macOS 12+, Ubuntu 20.04+에서 실행 가능하다.
- 인터넷 없이도 동작한다 (모델은 최초 다운로드 후 로컬 캐시 사용)

### NFR-04: 확장성

- LSTM 모델을 사전 학습 모델로 교체 가능하게 설계한다.
- 향후 AWS 리소스 연동이 가능하도록 모듈을 분리한다.
- 감정 카테고리 추가/수정이 용이한 구조로 설계한다.

### NFR-05: 사용 편의성

- 설치 및 실행이 간단해야 한다 (`pip install` + UI 실행)
- UI가 직관적이어야 한다.
- 오류 발생 시 사용자에게 명확한 안내 메시지를 제공한다.

---

## 5. 기술 스택

| 분류 | 기술 | 용도 |
|------|------|------|
| 언어 | Python 3.9+ | 메인 개발 언어 |
| 영상처리 | OpenCV | 카메라 캡처, 영상 표시 |
| 얼굴 감지/분석 | MediaPipe 또는 DeepFace | 표정 분석 및 감정 분류 |
| 음성 처리 | librosa | 오디오 특징 추출 (MFCC, 피치 등) |
| 오디오 캡처 | PyAudio 또는 sounddevice | 마이크 입력 |
| 음성 합성 | pyttsx3 또는 edge-tts | 음성 안내 출력 |
| 수치 연산 | NumPy, Pandas | 데이터 처리 및 통계 |
| 딥러닝 | TensorFlow 또는 PyTorch | LSTM 모델 학습/추론 |
| UI | Streamlit 또는 PyQt | 설정 및 결과 표시 UI |

---

## 6. 제약사항

- 초기 MVP에서는 AWS 리소스를 사용하지 않는다.
- 모든 처리는 로컬 PC에서만 수행한다.
- GPU가 없어도 동작해야 한다.
- 카메라 또는 마이크가 없는 환경에서는 해당 기능만 비활성화하고 나머지는 동작한다.
- LSTM 초기 버전은 더미/샘플 데이터 기반으로 구현한다.
- 네트워크 소켓을 열지 않는다 (외부 통신 없음).
