# requirements-face.txt
# 표정 분석 기능 (face mode, full mode)
# Python 3.11 권장
# ⚠️ Python 3.13에서는 TensorFlow 호환 문제로 미검증입니다.
#
# 설치: pip install -r requirements-face.txt
# 의존: requirements-minimal.txt가 먼저 설치되어 있어야 합니다.

# DeepFace (표정 감정 분류)
deepface>=0.0.90

# TensorFlow (DeepFace 내부 의존)
# deepface 설치 시 자동 포함됨
# 별도 지정 시: tensorflow>=2.15.0,<3.0.0
