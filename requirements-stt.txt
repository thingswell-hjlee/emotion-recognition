# requirements-stt.txt
# 한국어 음성 인식 (STT) 기능 - faster-whisper 기본
# Python 3.11 권장, Windows 11 호환
# ⚠️ Python 3.13은 미검증입니다. Python 3.11을 사용하세요.
#
# 설치: pip install -r requirements-stt.txt
# 의존: requirements-voice.txt가 먼저 설치되어 있어야 합니다 (sounddevice 필요).
#
# ⚠️ whisper 모델은 최초 실행 시 자동 다운로드됩니다 (tiny: ~40MB, base: ~75MB, small: ~250MB)
# ⚠️ CPU 환경에서 small 모델은 느릴 수 있습니다. 일반 노트북에서는 tiny 또는 base 권장.
#
# openai-whisper를 사용하려면 별도로 설치하세요:
#   pip install -r requirements-stt-openai-optional.txt

# 빌드 도구 (Windows에서 필요)
setuptools==69.5.1
wheel==0.43.0
packaging==23.2

# protobuf (CTranslate2 의존성 - pip이 자동 해결하도록 넓은 범위 지정)
# Note: faster-whisper/ctranslate2가 자체적으로 호환 버전을 지정하므로
#       여기서는 최소 버전만 지정합니다. pip resolver가 최적 버전을 선택합니다.
protobuf>=3.20.0

# 기본 STT 엔진: faster-whisper (CTranslate2 기반, 빠르고 Windows 호환)
faster-whisper>=1.0.0
ctranslate2>=4.0.0
tokenizers>=0.15.0
huggingface-hub>=0.20.0
