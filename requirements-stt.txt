# requirements-stt.txt
# 한국어 음성 인식 (STT) 기능
# Python 3.11 권장
#
# 설치: pip install -r requirements-stt.txt
# 의존: requirements-voice.txt가 먼저 설치되어 있어야 합니다 (sounddevice 필요).
#
# ⚠️ whisper 모델은 최초 실행 시 자동 다운로드됩니다 (tiny: ~75MB, base: ~150MB, small: ~500MB)
# ⚠️ CPU 환경에서 small 모델은 느릴 수 있습니다. 일반 노트북에서는 tiny 또는 base 권장.

# 옵션 A: OpenAI Whisper (기본 권장)
openai-whisper==20231117

# 옵션 B: Faster Whisper (CTranslate2 최적화, 더 빠름)
# faster-whisper==1.0.1
# 둘 중 하나만 설치하면 됩니다. 기본은 openai-whisper입니다.
# faster-whisper를 사용하려면 위 주석을 해제하고 config에서 stt_engine_type="faster-whisper"로 설정하세요.
